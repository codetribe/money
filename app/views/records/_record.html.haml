%tr
  %td=record.description
  %td=record.time
  %td=record.type
  %td
    =record.sheet.currency
    =record.amount
    \/-
  %td
    =link_to "Edit", edit_record_path(record), class: "btn btn-mini"
    =link_to "Delete", record, method: :delete, class: "btn btn-mini", confirm: "Do you truly want to delete record?"

